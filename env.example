# GitHub Personal Access Token with repo scope
# Get one from: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Your GitHub username
GITHUB_USERNAME=your_username_here

# Theme for the generated card (optional, defaults to github_dark)
# Available: github_dark, github, dracula, nord_dark, tokyo_night, radical, monokai, gruvbox, synthwave, catppuccin
THEME=github_dark

# Filter settings for excluding mass data uploads
# Comma-separated list of file extensions to exclude (optional)
EXCLUDE_EXTENSIONS=json,csv,lock,min.js,min.css,svg,png,jpg,jpeg,gif,ico,woff,woff2,ttf,eot

# Maximum lines per file to count (files with more additions are considered data dumps)
MAX_LINES_PER_FILE=2000

# Output directory for generated cards (optional, defaults to ./output)
OUTPUT_DIR=./output
